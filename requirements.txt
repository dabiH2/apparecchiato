# Core simulation + planning (everything except the model backends)
numpy>=1.26
mujoco>=3.2.0

# Video for the demo reel
imageio>=2.34
imageio-ffmpeg>=0.4.9
pillow>=10.0

# Intel inference path (the challenge's deployment target)
openvino>=2026.3
optimum[openvino]>=1.21
transformers>=4.44
torch>=2.3            # CPU wheel is enough: OpenVINO does the inference
# On Windows/Linux CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Speechmatics voice interface (bonus track)
websockets>=12.0
sounddevice>=0.4.6    # microphone input only; not needed for --voice file

# Dev
pytest>=8.0
